# Local DB overrides. Copy these into .env when using the local Docker Postgres.
# Or keep a separate .env with DB_HOST=localhost for local development.

DB_HOST=localhost
DB_PORT=5432

# If you use DATABASE_URL, uncomment and use this instead of DB_* for local:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/horilla
