# Copy this file to .env on the NAS when using Container Station UI.
# Set PROJECT_PATH to the full path of your project folder on the QNAP.
# Find the path in File Station (right-click project folder) or SSH: cd to project, run pwd.
#
# Example (edit to match your path):
PROJECT_PATH=/share/Public/geekonomy-hrms
